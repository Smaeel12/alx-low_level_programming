/**
 * set_string - function that sets the value of a pointer to a char.
 * @s: the value of a pointer.
 * @to: the pointer where to set the value.
 */
void set_string(char **s, char *to)
{
	*s = to;
}
